    // frontend/src/config.js

    /**
     * @file Arquivo de configuração para o frontend.
     * Contém variáveis globais como a URL base da API.
     */

    /**
     * A URL base da sua API de backend.
     * Altere esta URL se o seu backend estiver rodando em um endereço diferente
     * (por exemplo, em um servidor de produção).
     * @type {string}
     */
    export const API_URL = "http://localhost:8000";
    